export interface EventType {
  id: string;
  slug: string;
  title: string;
  durationMin: number;
  description?: string;
  color: string;
  active: boolean;
  createdAt: string;
  updatedAt: string;
}
