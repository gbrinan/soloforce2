// Reserved for future expansion (per-app advanced settings panel).
// MVP keeps settings inline in AppManagerPopup. Exported as a no-op placeholder so future imports don't break.
export default function AppSettingsPanel(_props: { appId: string; onClose: () => void }): null {
  return null;
}
