{
  "_comment": "핵심 라우트·정적 자산 골든 목록. 리팩토링으로 마운트가 유실되면 route-golden-test가 잡는다. 새 핵심 표면 추가 시 여기에 등록.",
  "routes": [
    "/api/health",
    "/api/staff",
    "/api/voice/health",
    "/api/jobs",
    "/api/approvals"
  ],
  "staticAssets": [
    "/vad/silero_vad_legacy.onnx"
  ],
  "_hookNote": "spawn 코드(worker-pty·terminal-ws·agent.ts)가 참조하는 훅 스크립트. 없으면 approval 모드 MCP가 fail-open이 된다 (docs/toolbox-concierge/findings.md Issue 2).",
  "hookScripts": [
    "scripts/mcp-approval-hook.mjs",
    "scripts/stop-response-hook.mjs",
    "scripts/agent-event-hook.mjs"
  ],
  "sourceMustMention": {
    "/api/voice": "src/server",
    "/vad/": "src/server"
  },
  "_probeNote": "라이브 프로브는 실파일·실핸들러 경로로. 디렉토리 루트는 404가 정상."
}
